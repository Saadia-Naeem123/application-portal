const HOLIDAY_TYPE = Object.freeze({
  PUBLIC: 'PUBLIC',
  UNIVERSITY: 'UNIVERSITY',
  SPECIAL: 'SPECIAL',
});

const ALL_HOLIDAY_TYPES = Object.values(HOLIDAY_TYPE);

module.exports = { HOLIDAY_TYPE, ALL_HOLIDAY_TYPES };
